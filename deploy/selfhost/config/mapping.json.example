{
  "_note_baseUrl": "target.baseUrl is the server ROOT - scheme, host, port, NO path. The session URL is built as ${baseUrl}/.well-known/jmap (RFC 8620 2.2), so a trailing /jmap here asks for /jmap/.well-known/jmap and 404s in a way that looks like the server is wrong. This example carried that mistake until 2026-08-08.",
  "_note": "Self-host mapping example (workplan 0010). Copy to a real name ending in .json in this directory (e.g. mapping.json) and edit. Files ending in .example are NOT loaded. Every *.json here is scheduled on startup. Credentials are referenced by ENV VAR NAME (tokenFromEnv / passwordFromEnv), never inline — set those variables in deploy/selfhost/.env.",
  "tenantId": "00000000-0000-4000-8000-000000000001",
  "mappingId": "11111111-1111-4111-8111-111111111111",
  "schedule": { "cron": "*/15 * * * *" },
  "_note_tls": "source.tls (and target.tls on an imap-dav target) is OPTIONAL and defaults to TRUE - implicit TLS on connect, whatever the port. Until 2026-08-09 it was deduced as port === 993, so an IMAPS server on any other port got a cleartext socket and a failure that read like a network fault. Set \"tls\": false only for a port 143 style cleartext/STARTTLS connection, and know that you are choosing it.",
  "_note_tlsVerify": "source.tlsVerify (and target.tlsVerify on imap-dav) is OPTIONAL and defaults to TRUE - the server certificate is verified. Set \"tlsVerify\": false ONLY for a dev/lab server with a self-signed certificate (the dev Stalwart is one). Never for a production mailbox: an unverified TLS socket hands the credentials to whoever answers first. Until 2026-08-09 the source skipped verification for everyone; a mapping that relied on that now needs this field.",
  "source": {
    "type": "imap-oauth2",
    "host": "outlook.office365.com",
    "port": 993,
    "user": "you@example.com",
    "auth": { "kind": "xoauth2", "tokenFromEnv": "SOURCE_OAUTH_TOKEN" }
  },
  "target": {
    "type": "jmap",
    "baseUrl": "https://mail.example.net",
    "user": "you@example.net",
    "auth": { "kind": "basic", "passwordFromEnv": "TARGET_JMAP_PASSWORD" }
  },
  "_note_domains": "The appliance runs five domains (mail/calendar/tasks/contacts/files). The mail domain uses the top-level source/target above. To also sync calendar/tasks/contacts/files, add a \"domains\" block with caldav/carddav/webdav source+target entries per domain, each { enabled, source, target }. Tasks share the CalDAV entry shape with calendars and point at the same DAV root: a task list IS a calendar collection, one that declares VTODO in its supported-calendar-component-set (RFC 4791 5.2.3), and discovery tells the two apart by that property rather than by address. See docs/selfhost-quickstart.md and packages/shared/src/config.ts for the schema."
}
