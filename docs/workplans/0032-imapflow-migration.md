# Workplan 0032 — migrate off `imap-simple` to `imapflow`

## Status — 2026-08-06 (update this block at the end of every session)

| Task | Status | Evidence |
|---|---|---|
| T0 Adapter seam + parity harness | ✅ **BUILT 2026-08-05 — and mutation-verified, because a harness that cannot fail is the whole risk** | `packages/connectors/src/imap-parity.ts` — `compareSources(a, b)` runs two `SourceConnector`s against the same server and reports every disagreement as a NAMED field on a NAMED message: folder set, per-folder `path`/`name`/`specialUse`, then per item `messageId`, `keywords`, `receivedAt`, `size`, `sourceRef`, the resume cursor, the `unkeyable` count, and a bytewise sample of message bodies. Items are keyed by `sourceRef` (`<folder>:<uid>`, the SERVER's uid) rather than by `messageId` — keying by the field under suspicion would make a normalisation difference look like "one message present, another missing", which is true, useless, and points at the wrong thing. Flags are compared as a SET; everything else verbatim, **especially `messageId`**, where trimming here would hide the exact class of difference the harness exists to catch. **13 unit tests, and they perturb rather than agree**: a client that strips angle brackets is caught, one that leaves trailing whitespace is caught, and so are a dropped flag, a different date, a missing folder, a misread special-use, a diverging cursor, an `unkeyable` disagreement and differing bodies — the last WITHOUT putting message bytes in the failure message (§17). **Mutation-verified twice**: making `normaliseField` trim the message id fails 2 tests, and dropping `messageId` from the compared fields fails 2 tests. `ParityResult` also carries `foldersCompared`/`itemsCompared`/`bodiesCompared`, because "no differences" over an empty mailbox is a tautology with a passing badge — the integration test asserts the counts, not just the agreement. **`imap-parity.integration.test.ts`** drives it against the Testcontainers Stalwart with a seeded mailbox (one plain id, one with a whitespace-padded header, one with none at all). Today it compares `ImapSource` with ITSELF and the file says so at the top: that is a smoke test of the harness against a live server, not evidence of parity. ~~**T1 turns it into the real gate with a one-line diff.**~~ **It did, on 2026-08-06: the second `source()` is now `imapFlowSource()`, and this file no longer compares a client with itself.** |
| T1 `imap-source.ts` (read path) | 🟢 **BUILT AND GREEN IN CI 2026-08-06 — `ImapFlowSource` ships BESIDE the proven client, and the harness is now the real gate that has actually run** | `packages/connectors/src/imapflow-source.ts` implements the same `SourceConnector` contract on `imapflow`, and `imap-parity.integration.test.ts` — which until today compared `ImapSource` with ITSELF and said so at the top — now runs the two against the same seeded Stalwart mailbox. That was the one-line diff T0 was built to make possible, and it is what makes this reviewable: a difference arrives as a NAMED field on a NAMED message rather than as a behaviour change found later in somebody's mailbox. **`imap-source.ts` gets exactly ONE behavioural change, named below (`RFC822.SIZE`), and no more** — the proven path still runs on `imap-simple` and nothing is cut over. (This row claimed "untouched in behaviour" until the first CI run made that untrue; corrected here rather than left standing.) Three helpers were EXPORTED from it (`messageIdFromEnvelopeValue`, `mapImapFlagsToKeywords`, `uidFromSourceRef`) and both sources now call the same code rather than two copies; `imap-source.ts`'s 12 unit tests stayed green through the extraction, which is what makes it a move rather than a rewrite. **The natural key is shared on purpose, and what that does NOT do is the point:** sharing removes the risk of OUR logic drifting between two files, and deliberately does not paper over a difference in what the two CLIENTS hand in — `imapflow` trims the ENVELOPE value and `node-imap` does not, so a padded msg-id still produces a real difference the harness names. **ONE DELIBERATE REFUSAL, and it is a scope decision rather than a client detail: imapflow's name-based special-use inference is switched OFF.** Where a server omits RFC 6154, imapflow matches folder NAMES against localised tables (`lib/special-use.js`) and will decide "Gelöschte Elemente" is `\Trash` with no server flag behind it. Adopting that would change which folders `excludeSpecialUse` keeps OUT of the migration *and* which folder the §11.1 deletion signal reads as the owner's bin — folders that migrate today would stop, and a folder nobody classified as a bin would start being read as evidence the owner deleted things. Whatever its merits, that is owner-visible and does not belong inside a client swap. Special-use is therefore derived from the server's own LIST flags, which is exactly what node-imap exposes as `attribs`, and a unit test pins it. **If the owner wants the richer inference it is a separate decision, not a follow-up commit.** **23 unit tests** covering what the harness cannot: the special-use refusal, the shared key helper (brackets kept, added, `unkeyable` counted and OMITTED when zero), the cursor arithmetic reproduced quirk-and-all, an empty mailbox never fetched, a listing failure never degrading to an empty listing, fetch-by-UID with `ImapSource`'s three-attempt retry, and exactly one token refresh on an auth failure. **One expectation was written wrong and the test caught it**, which is the only reason the quirk is now documented: with a cursor, `ImapSource` seeds `maxUidNext` from the CURSOR and bumps only on `uid > maxUidNext`, so the highest message re-lists on every subsequent pass. Harmless, and reproduced rather than fixed — a cursor that advanced differently is precisely the disagreement the harness would (correctly) report. **AND THE HARNESS EARNED ITS KEEP ON THE FIRST REAL RUN — the difference it named was a defect in the PROVEN client, not in the candidate.** CI reported `INBOX/items/INBOX:50 · size: '' vs 216` (and six more like it): `ImapSource` produced an EMPTY `MailItem.size` for every message and `ImapFlowSource` produced the real byte count. The cause is one missing option: node-imap only appends `RFC822.SIZE` to a FETCH when `options.size` is set (`Connection.js`), and `imap-source.ts`'s listing criteria asked for `bodies`/`struct`/`envelope` and not `size` — so `attrs.size` has always been `undefined`. **That is not cosmetic and it is not confined to the migration:** pre-sync discovery sums exactly this field (`itemBytes`, `apps/worker/src/orchestration.ts`) to tell the owner how much data is about to move on the §11.2 Review & confirm screen they approve before anything is copied — so the mail domain has been showing a count with no bytes behind it. The SYNC path is unaffected, because `reconcile.ts` already refuses the listing's advertised size in favour of the fetched message's own byte length (that comment records the same class of bug being fixed once before). **Fixed rather than reproduced.** The alternative was to drop `size` from the imapflow source so the two agreed on nothing, which is parity bought by discarding real data. So `LISTING_FETCH_CRITERIA` is now a named constant with `size: true`, pinned by a mutation-verified unit test (removing the line fails it), and the integration test gained a third case asserting every listed message actually CARRIES a size — because `compareSources` stringifies `size ?? ''`, so two clients reporting nothing agree perfectly and the gate would have gone green on the way back into the same hole. **One consequence for this row's own claim:** T1 no longer says `imap-source.ts` is untouched. It makes exactly ONE behavioural change to the proven client — that option — named here with its reason. **THE COMPARISON THEN RAN GREEN — run 31092829492, `integration-tests`, 2026-08-06.** `✓ packages/connectors/src/imap-parity.integration.test.ts (3 tests)` with `✓ ImapSource and ImapFlowSource agree, field by field, on a real mailbox`, inside `Test Files 45 passed (45) · Tests 376 passed (376)`. Quoted with the case name because "integration-tests passed" would not distinguish a comparison that agreed from one that never ran — the file's own skip branch is written to say which, and this is the positive evidence that it did not fire. **So every field the harness compares now agrees between the two clients on a real Stalwart mailbox**: the folder set and each folder's `path`/`name`/`specialUse`; per message `messageId`, `keywords`, `receivedAt`, `size`, `sourceRef`; the resume cursor; the `unkeyable` count; and a bytewise sample of message bodies. **The one that mattered is `messageId`** — the natural key, the field whose divergence would have re-copied every message on the next pass with every write succeeding — and it agreed including on the deliberately whitespace-padded fixture. That is the risk this whole workplan was written around, and it is now measured rather than argued. **What is NOT done: nothing is cut over.** No production path constructs `ImapFlowSource`; `imap-source.ts` remains the only mail source any deps builder builds. Cutting over is its own step behind a green parity run, and T2/T3 are unchanged. |
| T2 `imap-dav-target.ts` (write path) | 🟡 **BUILT 2026-08-06 — `ImapFlowDavMailTarget` ships BESIDE the proven writer, plus the WRITE-path parity harness T0 never built. NOT run here: no Docker, so CI is the first execution of the comparison** | 873 lines, and the one that can lose data. **T0's charter was half-delivered and this is the other half.** It promised a seam asserting "same folder list, same natural keys, same flags, same append outcome"; `imap-parity.ts` delivered the first three for the READ path and the append outcome waited for the write port. `imap-target-parity.ts` — `compareMailTargets(a, b)` — cannot be shaped like the read harness: two WRITERS cannot share a mailbox, because the first to append makes the second adopt and the comparison would measure ordering. So each writer gets its OWN mailbox, both are driven through the same script, and the OUTCOMES are compared: `ensureMailbox`, each `upsertEmail`'s `created`/`adopted`/`targetId` shape/`targetVersion`, a SECOND pass, `findByNaturalKey` (including for a message never written), `listEntries`, and `contentHashFor`. **Two comparisons carry the harness.** It reads `listEntries` COUNT rather than trusting the result objects — a writer can report `adopted` and have appended anyway, and only what is really in the mailbox settles it. And every content hash is compared **against the bytes that went in**, not only against the other writer, so two writers hashing the same wrong thing are still caught. **9 harness unit tests, and they perturb rather than agree**: a duplicate on the second pass, a writer that claims to adopt and appends anyway, a wrong hash, a missing `sizeBytes`, a lookup that answers "absent" for a message that is there, a non-UID `targetId`, a missing `targetVersion`, and the refusal to run both writers against one mailbox. **One of them failed first and the fake was wrong, not the harness**: the fake's `upsertEmail` read its own store directly instead of going through `findByNaturalKey`, so a blind lookup was a wrong ANSWER rather than a duplicate — a fake that does not share the real writers' control flow cannot model their failure modes. **THE FINDING: `imapflow`'s `messageDelete` silently widens the one destructive operation in this product.** Its own source computes `byUid = options.uid && hasCapability(connection, 'UIDPLUS')`, so on a server WITHOUT UIDPLUS, `messageDelete(uid, {uid: true})` issues a **bare EXPUNGE** — which removes every message in the mailbox carrying `\Deleted`, including ones another client flagged and never committed. That is data nobody in this product ever looked at, and hard rule 2 forbids touching it. `ImapDavMailTarget` refuses in that case; the port checks the capability HERE, before the call, and a unit test asserts `messageDelete` is never even reached without it. **A second, quieter one:** imapflow reports a refused MOVE or EXPUNGE by RETURN VALUE — its expunge command catches its own error, logs a warning and returns `false` — so both return values are checked and the `verifyGone` read-back stays regardless. **One deliberate behaviour difference, stated rather than smuggled:** the old `findByNaturalKey` issues one HEADER FETCH PER MESSAGE and string-scans for a line starting `message-id:`; this does one `FETCH … ENVELOPE` for the whole mailbox. Faster by the size of the mailbox, and more correct in one case — a Message-ID FOLDED across two lines is missed by a line-prefix scan and parsed correctly by ENVELOPE, so the old path would fail to find such a message and APPEND A DUPLICATE. The difference is latent (every message this product writes carries an unfolded id) and is recorded here rather than planted in the parity fixture, because manufacturing a red for a pre-existing defect nothing has hit would say something untrue about the migration. **30 connector unit tests, mutation-verified ELEVEN ways, and the eighth is the one worth reading.** Caught: dropping the UIDPLUS refusal (1), ignoring `messageDelete`'s return value (1), ignoring `messageMove`'s (1), dropping the `verifyGone` read-back (1), falling back to the UID as a natural key on reindex (1), reporting a UIDVALIDITY change as `conflicted` instead of throwing (1), and letting UIDVALIDITY reach the ledger as a bigint literal (1). **The eighth SURVIVED on the first attempt: swallowing a lookup failure as "not present" passed all 26 tests.** The only lookup-failure test was exercising the lock-acquisition catch and never reached the FETCH branch the mutation targets — a vacuous test, in a test written to guard against exactly that shape, and known only because the mutation was run rather than assumed. Two tests were added (the fetch failure, and the end-to-end consequence that it must not become a duplicate append) and the mutation now fails both. **THE HARNESS FAILED ON ITS FIRST REAL RUN AND NAMED TWO DEFECTS — one in the NEW writer, one in the PROVEN one.** CI reported `entries · count: 3 vs 0`, `find/… · found: true vs false` on every message, and `entries · every entry carries a size: false vs true`. **(1) The new writer, and it was mine.** `client.mailbox.exists` is a SNAPSHOT taken when the mailbox was SELECTed, and imapflow does NOT re-SELECT one that is already open — so `upsertEmail`, which selects the mailbox to read UIDVALIDITY while it is still empty and then appends, left `exists` at 0. Both `findByNaturalKey` and `entriesIn` short-circuited on that stale value and answered *empty* for a mailbox with three messages in it. That is precisely the shape hard rule 9 forbids, written by the same hand that wrote the comments warning about it: a stale optimisation answering "not present", which `upsertEmail` turns into a duplicate APPEND, and which reindexes a whole account as empty. Fixed by removing the guard entirely and fetching a **UID** range — RFC 3501 says a UID FETCH matching nothing returns no data rather than an error, which is the only thing the guard was protecting against. **The unit tests could not have caught it, and that is its own finding:** the fake refreshed `exists` on every lock, so a stale-`exists` short-circuit looked correct in all 26 of them. The fake now models the snapshot, a regression test appends-then-finds through one open mailbox, and reinstating the guard as a mutation fails 4 tests. Second time in this task that the FAKE was the thing that was wrong. **(2) The proven writer, same class as T1's finding.** `listEntries` fetched `{ bodies: ['HEADER'] }` with no `size`, and node-imap only appends RFC822.SIZE when `options.size` is set — so every `TargetEntry` it produced carried no `sizeBytes`, leaving §20's `totalBytesTarget` unmeasurable for every IMAP target. Its own comment claimed node-imap "reports RFC822.SIZE on every fetch"; the measurement says otherwise. Now `REINDEX_FETCH_OPTIONS`, a named constant with `size: true`, pinned by a mutation-verified test. **That makes two RFC822.SIZE defects the two harnesses found in one day, on opposite sides of the same connector pair.** **`imap-dav-target.ts` is otherwise untouched in behaviour**: two helpers were EXPORTED (`KEYWORD_TO_FLAG`, `extractMessageIdFromRfc822`) so the two writers cannot disagree about the flag mapping or the bracket-stripping convention, and its 16 unit tests stayed green through the extraction. **Nothing is cut over.** |
| T3a **The cutover** | 🟡 **DONE 2026-08-06 — production runs on `imapflow`. NOT yet proven by the e2e, which is the acceptance gate** | Four `new` expressions, and they are the migration: `buildImapSource` and `buildTargetWriter` in `build-deps.ts` (self-host), and their two counterparts in `build-deps-from-mapping.ts` (managed). Both editions flip together — hard rule 5 — and the config shapes are identical, so nothing else moved. **What it rests on, stated so it can be judged rather than trusted:** `imap-parity.integration.test.ts` and `imap-target-parity.integration.test.ts` both run on every push and are both green — the read path agreeing on the folder set, per-folder `path`/`name`/`specialUse`, per message `messageId`/`keywords`/`receivedAt`/`size`/`sourceRef`, the cursor, the `unkeyable` count and a bytewise body sample; the write path agreeing on `ensureMailbox`, every upsert's `created`/`adopted`/`targetId` shape, a SECOND pass, `findByNaturalKey`, `listEntries` by COUNT, and every content hash against the bytes that went in. **`messageId` is the one that mattered** — `naturalKeyForItem()` hashes it, so a difference there re-copies every message on the next pass with every write succeeding. **Pinned by `toBeInstanceOf(ImapFlowSource)` in both builders' unit tests**, so a revert is loud rather than silent. **The e2e has NOT run on this path yet.** `e2e.yml` is nightly (01:30 and 03:30 UTC, both persistence backends) plus manual dispatch, and this workplan's own acceptance criterion is *"the migration is done when that is still true"*. Until a nightly is green on the flipped path, this row is 🟡 and not ✅. |
| T3b Drop the `imap-simple` dependency | ⬜ **Deliberately NOT done in the same change as T3a, and the reason is the point** | 6 test files and 4 manifests still declare it. **Dropping it deletes the reference implementation both parity harnesses compare against** — do it alongside the cutover and the comparison becomes `ImapFlowSource` against itself, which is exactly where T0 started and what that file calls *"not evidence of parity"*. That would remove the safety net at the one moment it first becomes load-bearing: the pass immediately after a live cutover. So `imap-simple`, `ImapSource` and `ImapDavMailTarget` stay, exported and tested, guarding a path that is now REAL rather than hypothetical — if the two clients ever diverge, CI says so while the proven one is still there to fall back to. **The trigger is a green nightly e2e on the flipped path** (either backend). When that lands, this task deletes the two old connectors, both harnesses, the six test imports and the four manifest entries in one change — and states plainly what is being given up, because after it there is no second implementation to compare against and the guarantee reverts to ordinary tests. |
## Why this exists

**Owner decision 2026-08-05 (0026 T3 row 21): migrate.** ADR-0022 said to move
off `imap-simple` and called it "not urgent"; no decision was ever recorded and
the dependency is still declared in four manifests. `imap-simple` is a thin
promise wrapper over the unmaintained `imap` package; `imapflow` is actively
maintained and materially better at IDLE and at large mailboxes.

**Why it gets a workplan instead of an afternoon.** This is 1430 lines of
production code across the two connectors, plus six test files, and the APIs are
not similar: `imapflow` is async-iterator based, and its mailbox handling,
`append` semantics and flag operations all differ from `imap-simple`'s. More to
the point, **this is the path proven in the nightly e2e** — the one thing in
this product with end-to-end evidence behind it — and a rewrite that lands in
one commit trades that evidence for a hope.

## T0 comes first, and it is the whole safety argument

Before either connector is touched, build a seam that lets **both clients run
against the same fixtures** and assert identical results: same folder list, same
natural keys, same flags, same append outcome.

That harness is what makes the migration reviewable. Without it, "imapflow
behaves the same" is an assertion; with it, a difference shows up as a failing
comparison naming the operation rather than as a silent behaviour change
discovered in somebody's mailbox.

**The specific risk it exists to catch:** the natural key. `imap-source.ts`
produces `internetMessageId`, which `naturalKeyForItem()` hashes and which is
what makes an IMAP↔Graph transport switch safe. If `imapflow` normalises that
header differently — whitespace, angle brackets, casing — every message
re-copies on the next pass, and every write succeeds while it happens.

## Order, and why

**T1 before T2.** The read path can be wrong loudly (items missing, counts off)
and the write path can be wrong quietly (a duplicate, a lost flag, an append
that silently truncated). Doing the loud one first means the harness is trusted
before it guards the dangerous half.

**Update 2026-08-06 — and T1 itself is split the same way.** Building
`ImapFlowSource` and CUTTING OVER to it are two steps, not one. The connector
ships beside the proven client with nothing in production constructing it, so
the only thing this change can break is a test. The cutover is a separate
commit behind a green parity run against a mailbox with something in it —
which is also why the harness asserts `itemsCompared` rather than only
`differences`.

**T3 last, and it is not bookkeeping.** The dependency stays declared until
every consumer is migrated, because a half-migrated tree that no longer declares
the old client fails at runtime rather than at install. Note that
`packages/ledger` declares `imap-simple` as a **devDependency** used only by
`shadow-pass.integration.test.ts` — that is correct placement, not a stray, and
it goes when the test does.

**Update 2026-08-06 — T3 turned out to be two tasks, and separating them is a
safety property rather than tidiness.** Doing the cutover and the dependency
drop in one change deletes the reference implementation the parity harnesses
compare against, at the exact moment the comparison first guards something real:
the first production pass on the new client. A harness left running
`ImapFlowSource` against itself is what T0's own file calls "not evidence of
parity". So T3a flips the call sites and keeps everything else; T3b removes the
old client once a nightly e2e has been green on the flipped path. The order is
the same argument the whole workplan is built on — the swap lands after the
evidence, not with it.

## Hard rules that bite here

- **Rule 1 (idempotency):** a natural key that changes under the new client is
  not an error, it is a re-copy of every message — and a successful one.
- **Rule 9:** where `imapflow` cannot do what `imap-simple` did, the connector
  says so. A silently narrower capability is the worst outcome of a swap like
  this.
- **The e2e is the acceptance test.** Not "the unit tests pass" — this path's
  value is that it has run against real servers nightly, and the migration is
  done when that is still true.
