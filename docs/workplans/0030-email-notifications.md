# Workplan 0030 — email notifications: ad hoc + daily/weekly attention summaries

## Status — 2026-08-03 (update this block at the end of every session)

| Task | Status | Evidence |
|---|---|---|
| T1 The channel: SMTP per edition, EN/NL templates, recipient locale | 🟡 **Rules + templates built 2026-08-03; transport and wiring are the next slice** | `packages/shared/src/notifications.ts`: the `Notifier` port (one method, so no policy can accumulate in a transport), the ad hoc `NotificationEvent` union, and EN/NL templates beside each other with **compile-time key parity** (`DigestLines`/`EventLines` interfaces — a line missing from one language is a type error, the 0024 T1 property). The two load-bearing rules are here and exhaustively tested: **an empty digest returns `undefined`** (a weekly "all clear" trains its reader to filter the channel, taking the one that mattered with it — so silence IS the signal), and **a blind spot is never silence** (a mapping whose queue could not be READ sends anyway, with the server's reason verbatim, because "I found nothing" and "I could not look" must not be the same email — rule 9). Prose boundary honoured: a decision's `summary` and a run's `lastError` ride verbatim in both languages. 14 unit tests. **Wiring landed the same day**: `MailTransport` is a function type in shared (so the browser bundle, which imports shared, never meets a mail library) and `packages/connectors/src/smtp-transport.ts` is the ONE nodemailer import in the workspace — chosen over hand-rolling SMTP+STARTTLS+AUTH, MIT, pure JS with no native build step, so ADR-0019's binary-free property holds. `readNotifierConfig(env)` turns the environment into a channel and distinguishes **nothing set** (the normal default, stated plainly) from **half set** (somebody TRIED — it names the missing variables rather than staying quietly off, rule 9); ports default to 587/STARTTLS or 465 when `SMTP_SECURE`. `disabledNotifier` says why ONCE, never throws; `createNotifier` **propagates** send failures rather than swallowing them (a notification that silently failed to send is indistinguishable from one never worth making). The appliance builds one at startup from `.env` (rule 3; rule 5 — the owner's own SMTP, nothing managed), announces ON/OFF with the reason, and exposes it on `SelfhostHandle.notifier` so T2/T3's events have a seam to call. Documented in `selfhost.env.example`. 32 unit tests across rules, config and appliance wiring. **Still no caller and no schedule** — T2 (ad hoc events) and T3 (the digest + cadence) supply those; recorded here rather than left to look finished. |
| T2 Ad hoc events (decision raised, run failing, verify done, finished) | 🟡 **Three of four wired 2026-08-03 — the three with live sources** | `runs_failing`: a pass where EVERY domain failed (the same definition the run row uses — a partly failed pass is per-item trouble the queues already report), plus a pass that threw outright. Deduped by `createFailureStreakGate`: silent below the threshold (one bad pass is usually a blip), speaks **exactly at** 3 consecutive failures and never again during that outage — a per-minute cron would otherwise send sixty emails about one unplugged server — and **resets on recovery**, so a later outage is reported again rather than the channel going permanently quiet after its first bad day. `verification_finished`: once per run per mapping, from the scan closure; only `PASS` counts as passed (WARN is not a green light, and the one email somebody reads about it is the worst place to blur that). `migration_finished`: on the real transition only — the repeat path answers `alreadyDone` and returns before reaching it, so finishing twice cannot send twice. All three go through `tell()`, which logs a failed SEND loudly but never rethrows: a migration that finished is still finished if the email about it bounced. **`decision_raised` is deliberately NOT wired** — nothing raises decisions until 0028's detectors exist (blocked on 0027 T0's admin consent), and wiring a notification to a seam with no caller is the dead-surface shape 0026 spent a day deleting. 5 gate tests; 1350/1350 unit. |
| T3 The daily/weekly "needs attention" digest | ✅ **Both editions 2026-08-03** — appliance in T3, managed in T4 | The digest is now collected from the SAME ledger calls and the SAME filters the queue screens use (`summariseQueues`, in `@openmig/shared` — it started in `apps/selfhost` and moved when T4 gave managed a digest that had to count identically): confirmed-and-unacknowledged deletions, unacknowledged moves, failures that have exhausted retries (`needsDecision`), pending drift decisions counted **once per tenant** (a new mailbox belongs to no mapping yet, so several mappings must not each claim the same decision), and `readyForCutover`. Never a parallel count — a digest that said four and a queue that showed three would send the owner hunting for an item that does not exist, and the next digest goes unread. Split into a pure function precisely so that property can be pinned without a database: 13 unit tests hold the filters to the endpoints' expressions. Every read is individually guarded and a failed read becomes a **blind spot carrying the server's words**, not a zero, so `wantsAttention` sends on it alone (hard rule 9); a `done` mapping is dropped **before** its four queries (`reportsToDigest`) — a finished migration keeps its history but stops nagging, or every appliance that ever completed one would email about it forever. Cadence comes from `NOTIFY_DIGEST` (daily | weekly | both | off, default **daily**); an unrecognised value falls back to daily deliberately — a typo that sends an email a day is a visible mistake somebody fixes, a typo that produces silence is the failure nobody notices. `off` leaves the ad hoc events alone. `digestSchedule()` decides which jobs exist (nothing at all when the channel is off, rather than a job that rediscovers that every morning) and the appliance registers them through the same `Scheduler` seam the syncs use, so a digest cannot outlive a shutdown: `0 8 * * *` and `0 8 * * 1`, morning local time — a summary landing at 03:00 is read twelve hours late, and a weekly recap arriving Friday afternoon is history rather than a to-do list. Documented in `selfhost.env.example`. 22 new unit tests (13 counting, 9 cadence/schedule) plus a real appliance boot with both digests registered, which is what proves the cron strings parse and the jobs stop with the process. **Follow-up 2026-08-03:** the collection LOOP had no test either — it moved behind a `CollectDeps` seam (`apps/selfhost/src/digest-collect.ts`, the twin of managed's `managed-digest-run.ts`) and **14 tests** pin it: a `done` mapping skipped before its reads (and never querying them again for the life of the appliance), a mapping whose STATUS could not be read still reported (unknown is not done — that is precisely the mapping worth mentioning), several blind spots collected rather than stopping at the first, the queues that COULD be read still counted, and the decision count taken once per tenant — including the case that would silently drop it, where the tenant's first mapping is `done` and a later one must carry the count. Where these tests assert the same rule as managed's in the same words, that is the point rather than duplication: the two editions collect differently and must MEAN the same thing. **Gap closed 2026-08-03:** `apps/selfhost/src/selfhost-queues.integration.test.ts` boots the appliance against the Testcontainers **Postgres** — PGlite's single connection is what made this impossible, since a test cannot seed rows around the running server — seeds seven items covering every state (a confirmed deletion and an answered one, an open move and an acknowledged one, a failure that has exhausted its retries and one still trying, and one perfectly ordinary migrated item that must appear in no queue at all), and holds the endpoints and the collector to **each other**: `deletionsWaiting` equals what `/deletions` shows, `movesWaiting` equals `/moves`, `failuresWaiting` equals `/failures`. The claim that the digest counts what the screens count is now a test rather than an inference from both callers using `summariseQueues`. It also proves the decision queue end to end over HTTP for the first time (T1 could only prove it empty). `uuidFromString` — the appliance's deterministic id derivation, which the test must compute to seed anything — moved from two copies with a comment asking them to match into one exported function. |
| T4 Preferences + the rollback flag + doc truth | ✅ **Done 2026-08-03 — and it closed T3's managed gap on the way** | **The rollback flag is real.** `notifyUsers` on `run-rollback` was an honest refusal since 0026 T1; it now sends `rollback_finished` through the same channel every other event uses. Three deliberate shapes: the channel is built **before** any rollback action, so an unconfigured SMTP is discovered while everything is still untouched and the caller can resubmit without the flag (discovering it afterwards would leave a system rolled back with nobody told); the send happens **after** the rollback, so the mail only ever describes something that actually happened; and a failed send is logged loudly but does **not** throw — a mail server that is down must not report a successful rollback as failed. The body says what is true NOW (old system authoritative, syncing resumed) before it says why, because that is the question somebody reading it at 22:00 actually has, and it refuses to claim the MX record was restored — DNS is verify-only. Still `default: false`: a rollback is an emergency and mail to every recipient is not something to do because nobody said not to. **Preferences are real because managed now has a digest.** A per-tenant cadence that nothing read would have been the dead surface 0026 spent a day deleting, so T4 built the `managed-digest` task first — ONE daily task that asks each tenant whether today is their day (`digestDueToday`, Monday for weekly), rather than a job per cadence that would need re-registering whenever a customer changed a dropdown. It reuses the appliance's counting exactly: `summariseQueues`/`reportsToDigest` moved into `@openmig/shared`, and managed reads through the **same `PgLedger` calls** rather than re-expressing the filters in SQL, which would have been a second copy free to drift. Recipients are the tenant's active owners and admins; a tenant with none is named in the log rather than silently skipped. One tenant's mail server refusing does not stop the others'. Preferences live in the existing `tenant.settings` JSON (no migration for one enum), read defensively — an unreadable value falls back to the **default, never to silence**, because a typo must not be why somebody stops hearing about their own migration — and written through a dedicated owner/admin route that **merges**, so saving a cadence cannot drop the slug. The Tenants screen card shows what is stored, disables itself when the tenant read FAILED (offering a default there would overwrite something nobody has seen), and shows the server's refusals verbatim. EN/NL both ways. **Doc truth:** SAD §11.2 #4 carries a dated note recording the scope (email only, no in-app centre) with the empty-digest and blind-spot rules, §23's bilingual-notifications transfer is marked **discharged**, the SAD is v1.4, and the CLI's stale "user notification is not implemented" line now says what is true of that command rather than of the product. 29 new unit tests (14 preference rules, 6 the env-builder, 6 the Tenants card, 3 the rollback event). **Follow-up the same day:** `managed-digest` shipped as ~200 lines of untested orchestration that emails other people's customers, which was the wrong shape to leave — its decisions moved behind a `DigestDeps` seam (`managed-digest-run.ts`, the task file is now only the Pool, the ledger, the transport and the cron) and **19 tests** pin them without a database: whose day it is (and that a not-due tenant's ledger is never even walked), that recipients are the tenant's own owners and admins in the tenant's own language, that nothing waiting sends nothing, that a `done` mapping's queues are never read, that a failed read becomes a blind spot carrying the database's words and forces a send while the OTHER queues still count, that a pending decision is counted once per tenant rather than once per mapping, and that one tenant's refused send costs that tenant only — never counted as sent. Writing them surfaced a real hole: a tenant whose every mapping is `done` carries its pending decisions nowhere, because the digest is a list of mappings. Rather than invent a row with a mapping id nobody can open, that case now **warns in the operator log** and the workplan records it — a known hole stated out loud beats a quiet one. 1422/1422 unit.

## Why this exists

Owner decision 2026-08-02 (0026 T3 row 5): **keep, scoped** — email only,
"with ad hoc, daily and weekly summary of what needs attention." SAD §11.2
#4/§5 promise the owner is told when the migration needs them; reality is
honest not-implemented stubs, a `notifyUsers: true` rollback flag that does
nothing (0026 T1 item 3 fixes its lie independently), and 0024's recorded
transfer: **notifications are bilingual from day one**.

The product reason: an SMB owner mid-shadow-sync checks the UI weekly at
best. The drift decision queue (0028) and the item queues only work if
"something is waiting on you" reaches an absent owner — that is an email,
not a bell icon. **No in-app notification center** — out of scope by this
decision.

Sequencing: after 0028's plumbing exists (its "decision raised" is the
flagship ad hoc event), though T1 and the digest's queue-count sources
predate 0028 and can start first.

## Tasks

- **T1 — the channel.** One `Notifier` seam in core; SMTP transport
  configured per edition (managed: operator SMTP via env secrets, rule 3;
  appliance: owner-supplied SMTP settings — self-host keeps working with
  no managed dependency, rule 5; unconfigured = notifications off, said
  honestly in the UI, never silently). Templates are typed EN/NL pairs in
  `@openmig/shared` beside each other (the 0024 T1 pattern — they cannot
  drift apart silently); the recipient's locale is a **per-member
  preference** (the small server-side column 0024's note anticipated;
  tenant default = the appliance operator's UI locale). Send failures
  surface as run/queue errors, verbatim (rule 9) — a notification that
  silently failed to send is the worst kind of missing.
- **T2 — ad hoc.** The immediate events, each deduped so one condition
  emails once, not per poll: a decision was raised (0028's seam), a
  mapping's runs are repeatedly failing, a verification run finished
  (either verdict), the migration finished. Event → template → recipients
  (owner/admin members; the appliance's configured address).
- **T3 — the digest.** "What needs attention", computed at send time from
  the same envelopes the screens read (never a parallel count): pending
  decisions (0028), waiting-on-you counts per queue (deletions / moves /
  failures), stalled or failing syncs, mappings sitting in
  READY_FOR_CUTOVER. Nothing needing attention = **no email** (an empty
  digest trains owners to ignore the channel). Daily and weekly cadences
  via the `Scheduler` seam — croner on the appliance, a scheduled
  Trigger.dev task on managed (the `managed-sync-tick` pattern).
- **T4 — preferences + truth.** Per-tenant cadence choice (ad hoc on/off,
  daily or weekly digest — owner/admin-editable on the Tenants screen,
  which 0026 T2 just made real, plus the appliance's config surface); wire
  `run-rollback`'s notify flag to the real channel once it exists (until
  then T1 item 3's honest refusal stands); dated notes in SAD §11.2 #4
  recording the scope (email-only, no in-app center) and 0024's bilingual
  requirement marked discharged.

## Hard rules that bite here

- **Rule 5:** the appliance edition notifies with nothing but the owner's
  own SMTP details; every piece lives behind the core seam.
- **Rule 3:** SMTP credentials are env/config secrets, never repo content.
- **Rule 9:** unconfigured SMTP and failed sends are stated, loudly; the
  digest never renders a blind spot as "nothing needs attention" — sources
  it cannot read are named in the email.
- **Rule 2/0024:** refusal and failure prose inside emails stays the
  server's verbatim words; the FRAME is the bilingual template.
