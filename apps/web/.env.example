# Optional: absolute API base for the dev server. Usually UNSET — `pnpm dev`
# proxies /api to localhost:3001 via vite.config.ts, and the Docker image's
# nginx does the same in a deployment, so the default baseURL '/api' works
# everywhere without configuration.
# VITE_API_URL=http://localhost:3001/api
